F = [0,1]
count = abs(int(input("How many Fibonacci-numbers do you want to know?")))
if count == 0:
    F = []
elif count == 1:
    F = [0]
while len(F) < count:
        y = F[len(F)-1] + F[len(F)-2]
        F.append(y)
print(F)
